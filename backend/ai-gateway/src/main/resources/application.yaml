spring:
  application:
    name: ai-gateway

server:
  port: 8090

grpc:
  client:
    parser-service:
      address: static://localhost:50051
      negotiation-type: plaintext

management:
  endpoints:
    web:
      exposure:
        include: health, info
